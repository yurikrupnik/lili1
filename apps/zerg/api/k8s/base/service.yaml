apiVersion: v1
kind: Service
metadata:
  name: zerg-api
  labels:
    app: zerg-api
spec:
  selector:
    app: zerg-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

