[tool.coverage.run]
branch = true
source = [ "zerg_job" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../../coverage/apps/zerg/job/html'
 --cov-report xml:'../../../coverage/apps/zerg/job/coverage.xml'
 --html='../../../reports/apps/zerg/job/unittests/html/index.html'
 --junitxml='../../../reports/apps/zerg/job/unittests/junit.xml'
"""

[project]
name = "zerg-job"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.9,<4"
readme = 'README.md'
dependencies = [
    "python-shared",
]

[tool.hatch.build.targets.wheel]
packages = ["zerg_job"]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []

fixable = ["ALL"]
unfixable = []

[tool.uv.sources]
python-shared = { workspace = true }
